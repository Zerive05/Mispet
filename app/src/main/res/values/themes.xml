<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mispet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Mispet" parent="Base.Theme.Mispet" />

    <style name="AppSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Hapus garis bawah -->
        <item name="queryBackground">@android:color/transparent</item>
        <!-- Warna teks hint -->
        <item name="android:textColorHint">#A6000000</item>
        <!-- Warna teks input -->
        <item name="android:textColor">@color/primary_blue_dark</item>
    </style>

    <style name="Theme.Mispet.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item></style>

</resources>